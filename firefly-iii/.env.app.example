# Firefly III - application example env
# This file follows the upstream `.env.example` layout but trimmed for Docker
# Copy to `.env` and adapt values for your environment. Keep secrets out of VCS.

# App environment
APP_ENV=local
APP_DEBUG=true
SITE_OWNER=mail@example.com

# Encryption key - must be exactly 32 chars or a base64-encoded 32-byte value
# Generate with: php artisan key:generate --show
APP_KEY=base64:CHANGEME32CharacterKeyRequiredForFirefly=

# Public facing URL (used to generate links and validate callbacks)
APP_URL=http://localhost:8080

# Localization / Timezone
DEFAULT_LANGUAGE=en_US
DEFAULT_LOCALE=nl_NL
TZ=UTC

# Trusted proxies (use ** in Docker setups)
TRUSTED_PROXIES=**

# Logging
LOG_CHANNEL=stack
APP_LOG_LEVEL=notice
AUDIT_LOG_LEVEL=emergency
AUDIT_LOG_CHANNEL=

# Database (use postgres/mysql/sqlite depending on your setup)
DB_CONNECTION=pgsql
DB_HOST=firefly-iii-application.postgres
DB_PORT=5432
DB_DATABASE=firefly_iii
DB_USERNAME=firefly_user
DB_PASSWORD=firefly_secure_password

# Redis
REDIS_SCHEME=tcp
REDIS_HOST=firefly-iii-application.redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_CACHE_DB=1

# OAuth / Passport (optional - leave empty to let the app generate keys)
PASSPORT_PRIVATE_KEY=
PASSPORT_PUBLIC_KEY=

# Static cron token (32 chars)
STATIC_CRON_TOKEN=PLEASE_REPLACE_WITH_32_CHAR_CODE

# Startup and tuning
DKR_CHECK_SQLITE=true

# Firefly specific
FIREFLY_III_LAYOUT=v1
QUERY_PARSER_IMPLEMENTATION=new

# Optional helpers for compose / local testing
VANITY_URL=http://localhost:8080/

# Notes:
# - For production set APP_ENV=production and APP_DEBUG=false
# - Use Docker secrets or environment files for DB and other credentials
