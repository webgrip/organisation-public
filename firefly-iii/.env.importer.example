## Importer - example env
## The importer is a small app that runs alongside Firefly III to import data.
## COPY the values you need to your runtime `.env` and protect secrets.

# Importer app key - 32-byte base64
IMPORTER_APP_KEY=base64:CHANGEME32CharacterKeyRequiredForImporter=

# Where the importer is reachable (used by nginx reverse proxy)
IMPORTER_BASE_URL=http://localhost:8080/importer

# Token used by importer to authenticate against Firefly III API
FIREFLY_III_ACCESS_TOKEN=

# If your Firefly III API is on a different internal host, set FIREFLY_III_URL accordingly,
FIREFLY_III_URL=http://firefly-iii-application.application:8080
